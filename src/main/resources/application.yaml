spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/parksmart
    username: "selmakaracic"
    password: ""
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: ${PORT:8080}